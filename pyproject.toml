[project]
name = "omniclaims-adjuster"
version = "0.1.0"
description = "Autonomous multimodal insurance claims adjuster powered by Google Gemini 3.1"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "OmniClaims Team", email = "team@omniclaims.ai"},
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
